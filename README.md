# MCTS

## mcts_tictactoe.cpp
这是基于蒙特卡洛树搜索（MCTS）算法实现的井字棋游戏 AI。代码定义了游戏状态、MCTS 节点以及 MCTS 算法的核心逻辑。

### 编译
```
mkdir build 
g++ -std=c++17 -o build/mcts_tictactoe mcts_tictactoe.cpp
```

### 运行
```
./build/mcts_tictactoe
```
